<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
        namespace="com.zimuka.peers.mapper.UserGroupMapper">
    <resultMap id="UserGroupMapperResultMap"
               type="com.zimuka.peers.dao.UserGroup">
        <id column="group_id" property="groupId"/>
        <result column="open_id" property="openId"/>
        <result column="app_id" property="appId"/>
        <result column="del_flag" property="delFlag"/>
        <result column="ct_time" property="ctTime"/>
        <result column="up_time" property="upTime"/>
        <result column="prepare" property="prepare"/>
    </resultMap>
    <sql id="Base_Column_List">
        group_id, open_id, app_id, del_flag, ct_time, up_time, prepare
    </sql>

    <insert id="save" parameterType="com.zimuka.peers.dao.UserGroup" useGeneratedKeys="true" keyProperty="id">
        insert into user_group
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="null != groupId">
                group_id,
            </if>
            <if test="null != openId">
                open_id,
            </if>
            <if test="null != appId">
                app_id,
            </if>
            <if test="null != delFlag">
                del_flag,
            </if>
            <if test="null != ctTime">
                ct_time,
            </if>
            <if test="null != upTime">
                up_time
            </if>
            <if test="null != prepare">
                prepare
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="null != groupId">
                #{groupId},
            </if>
            <if test="null != openId">
                #{openId},
            </if>
            <if test="null != appId">
                #{appId},
            </if>
            <if test="null != delFlag">
                #{delFlag},
            </if>
            <if test="null != ctTime">
                #{ctTime},
            </if>
            <if test="null != upTime">
                #{upTime},
            </if>
            <if test="null != prepare">
                #{prepare},
            </if>
        </trim>
    </insert>

    <update id="update" parameterType="com.zimuka.peers.dao.UserGroup">
        update user_group
        <set>
            <if test="null != appId">
                app_id=#{appId},
            </if>
            <if test="null != delFlag">
                del_flag=#{delFlag},
            </if>
            <if test="null != ctTime">
                ct_time=#{ctTime},
            </if>
            <if test="null != upTime">
                up_time=#{upTime},
            </if>
            <if test="null != prepare">
                prepare=#{prepare},
            </if>
        </set>
        where open_id=#{openId} and group_id=#{groupId}
    </update>

    <select id="findOneById" resultMap="UserGroupMapperResultMap">
        select <include refid="Base_Column_List"/>
        from user_group
        where open_id=#{openId} and group_id=#{groupId}
    </select>

    <select id="findUserGroupByParam" parameterType="com.zimuka.peers.dao.UserGroup" resultMap="UserGroupMapperResultMap">
        select <include refid="Base_Column_List"/>
        from user_group
        <where>
            <if test="null != groupId">
                and group_id=#{groupId}
            </if>
            <if test="null != openId">
                and open_id=#{openId}
            </if>
            <if test="null != appId">
                and app_id=#{appId}
            </if>
            <if test="null != delFlag">
                and del_flag=#{delFlag}
            </if>
            <if test="null != ctTime">
                and ct_time=#{ctTime}
            </if>
            <if test="null != upTime">
                and up_time=#{upTime}
            </if>
            <if test="null != prepare">
                and prepare=#{prepare}
            </if>
        </where>
    </select>

    <select id="findUsersByGroupId" resultMap="UserGroupMapperResultMap">
        select <include refid="Base_Column_List"/>
        from user_group
        where group_id=#{groupId} and open_id != #{openId}
    </select>

    <select id="findCardsOnGroupByOpenId" resultType="com.zimuka.peers.dto.CardsOnGroupDTO">
        select uc.id as id, uc.username as username, uc.user_wechat as userWechat, uc.user_company as userCompany, uc.user_industry as userIndustry,
         uc.user_city as userCity, uc.user_job as userJob, uc.user_img as userImg, uc.user_phone as userPhone, uc.user_email as userEmail,
          uc.synopsis as synopsis, uc.demand as demand, uc.resources as resources, uc.give_like_num as giveLikeNum
        from user_group ug, user_card uc
        where ug.open_id!=#{openId} and ug.group_id=#{groupId} and ug.open_id=uc.open_id
    </select>
</mapper>